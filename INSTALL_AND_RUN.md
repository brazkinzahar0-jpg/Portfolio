# 🚀 Автоматическая установка и запуск

## Самый простой способ

### Для Windows:

**Просто дважды кликните на файл:**
- `запустить.bat` - для обычной командной строки
- `запустить.ps1` - для PowerShell (более продвинутый)

Скрипт автоматически:
1. ✅ Проверит наличие Node.js
2. ✅ Попытается установить Node.js, если его нет
3. ✅ Установит все зависимости проекта
4. ✅ Запустит dev сервер
5. ✅ Откроет браузер автоматически (в PowerShell версии)

---

## Альтернативный способ (если скрипт не работает)

### Установка Node.js вручную:

1. Перейдите на https://nodejs.org/
2. Скачайте версию **LTS** (рекомендуется)
3. Установите (все настройки по умолчанию подойдут)
4. **Перезапустите командную строку/терминал**
5. Запустите скрипт `запустить.bat` снова

### Или установите через командную строку:

```bash
# Через winget (Windows 10/11)
winget install OpenJS.NodeJS.LTS

# Или через Chocolatey (если установлен)
choco install nodejs-lts -y
```

---

## После запуска

1. Откройте браузер
2. Перейдите на **http://localhost:3000**
3. Наслаждайтесь премиум портфолио! 🎉

---

## Остановка сервера

Нажмите **Ctrl+C** в окне командной строки

---

## Что делать, если возникли проблемы?

### Проблема: "Node.js не найден"

**Решение:** 
- Установите Node.js с https://nodejs.org/
- Перезапустите командную строку
- Запустите скрипт снова

### Проблема: Ошибка при установке зависимостей

**Решение:**
```bash
# Очистите кеш npm
npm cache clean --force

# Удалите папку node_modules
rmdir /s /q node_modules

# Запустите установку заново
npm install
```

### Проблема: Порт 3000 занят

**Решение:** 
Закройте другое приложение, использующее порт 3000, или создайте файл `.env.local`:
```
PORT=3001
```

---

## Прямой запуск (если Node.js уже установлен)

```bash
# Откройте PowerShell или CMD в папке проекта
cd "C:\Users\HOME\OneDrive\Desktop\портфолио"

# Установите зависимости (первый раз)
npm install

# Запустите сервер
npm run dev
```

---

**Готово! Просто запустите `запустить.bat` и всё заработает! 🎨✨**


